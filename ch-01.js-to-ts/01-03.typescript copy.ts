const firstName = "Georgia";
const nameLength = firstName.length();

function sayMyName(fullName) {
  console.log(`You action kingof shady, ain't callin' me ${fullName}`);

  sayMyName("Beyounce", "Knowles");
}
